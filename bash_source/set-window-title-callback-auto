if [ -z "${TITLE_SET_OK}" ]; then
	export TITLE_SET_OK=yes
	if [ -n "${REMOTE_PATH}" ]; then
		REMOTE_PATH="${REMOTE_PATH} -> $(hostname)"
	else
		REMOTE_PATH="$(hostname)"
	fi

    export REMOTE_PATH
fi

function set-window-title-callback-auto {
	echo "终端：${REMOTE_PATH} | $(pwd)"
}
